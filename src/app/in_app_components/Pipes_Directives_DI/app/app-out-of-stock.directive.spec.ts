import { AppOutOfStockDirective } from './app-out-of-stock.directive';

describe('AppOutOfStockDirective', () => {
  it('should create an instance', () => {
    const directive = new AppOutOfStockDirective();
    expect(directive).toBeTruthy();
  });
});
